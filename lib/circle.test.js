const Circle = require('./circle');
const chooseShape = require('./shapes');

const answers = { }

describe('Circle Class Test', () => {
    test('Check that chooseShape returns the circle SVG string', () => {
        
    })
})